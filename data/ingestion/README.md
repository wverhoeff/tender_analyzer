# Data Ingestion Directory

Drop raw Markdown files, costing sheets, and requirement tables here.

## Supported formats
- `.md` (Markdown format)
- `.txt` (Plain text format)

The system automatically reads files from this folder when initiating tender analysis runs.
